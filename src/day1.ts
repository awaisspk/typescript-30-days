const Multiply = (a: number, b: number): number => {
  return a * b;
};

function add(a: number, b: number): number {
  return a + b;
}

let a: number = null;
console.log(a);

type carProps = {
  name: string;
  model: string;
  year: number;
  charVoltage?: number;
};
